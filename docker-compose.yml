version: '3.8'

services:
  jupyter:
    build: .
    container_name: projet-leyenda
    ports:
      - "8888:8888"
      - "6006:6006"
    volumes:
      - .:/tf
    runtime: nvidia
    command: >
      jupyter notebook --ip=0.0.0.0 --port=8888 --no-browser --allow-root --NotebookApp.token=''
